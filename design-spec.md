# مواصفات الهوية البصرية — SafeScanPro

> يُطبَّق هذا التصميم على كل الشاشات الاثنتي عشرة المذكورة بملف المواصفات الأساسي، بدون استثناء. الهدف: هوية مميزة تعكس الدقة (المستند) والثقة (الخزنة الآمنة)، بعيداً عن قوالب التصميم العامة المكررة (كروت بظلال متطابقة، ألوان فينتك نمطية).

## 1. نظام الألوان (استخدم كمتغيرات Theme في Flutter، مو قيم مباشرة بكل ملف)

| الدور | القيمة (Hex) | الاستخدام |
|---|---|---|
| `backgroundPrimary` | `#FAFAF7` | خلفية كل الشاشات (أبيض دافئ، ليس أبيض ناصع) |
| `primaryInk` | `#1B2A4A` | الأزرار الأساسية، العناوين المهمة، أيقونات التنقل النشطة |
| `accentBrass` | `#C08B3D` | **حصراً** لعناصر الأمان: أيقونة القفل، شارة "محمي"، شاشة القفل، تأكيد النسخ الاحتياطي الناجح. لا يُستخدم كلون زخرفي عام |
| `accentScan` | `#2F7A6B` | خط المسح الحي بالكاميرا، مؤشر "تم الحفظ بنجاح"، حالة اكتمال العملية |
| `textPrimary` | `#22221E` | كل النصوص الأساسية |
| `textSecondary` | `#5C5B54` | نصوص ثانوية، تلميحات |
| `divider` | `#E4E0D8` | فواصل بين العناصر — **بديل** عن الكروت والظلال |
| `error` | `#B3423A` | حالات الخطأ فقط (لون أحمر-طوبي هادئ، ليس أحمر صارخ) |

**قاعدة صارمة:** لا تُستخدم أي كروت (Card widgets) بظلال متطابقة كنمط أساسي لعرض القوائم (المجلدات، المستندات). استخدم `ListTile` أو تصميم مخصص بفواصل خطية (`Divider` بلون `divider`) بدل ذلك. الكروت مقبولة فقط في حالات استثنائية محدودة (مثلاً بطاقة الترحيب بشاشة onboarding واحدة)، ليس كنمط متكرر.

## 2. الطباعة (Typography)

- **العناوين (Headlines):** خط `Manrope` (أو `IBM Plex Sans` إذا Manrope غير متاح بسهولة عبر `google_fonts` package) — وزن `Bold`/`SemiBold`
- **النصوص العادية:** نفس العائلة، وزن `Regular`/`Medium`
- **دعم العربي:** خط `IBM Plex Sans Arabic` أو `Cairo` — يجب أن يبدو بنفس جودة ووزن الخط الإنجليزي، لا فرق ملحوظ بالثقل البصري بين الواجهتين عند تبديل اللغة
- استخدم مكتبة `google_fonts` لتحميل الخطوط، مع تفعيل `GoogleFonts.config.allowRuntimeFetching = false` وتضمين الخطوط أوفلاين ضمن الحزمة (bundled assets) بما يتماشى مع مبدأ "بدون إنترنت مطلوب لأي وظيفة أساسية"

## 3. مبدأ التخطيط (Layout)

- **بدون نمط "كل شيء كرت"**: قوائم المجلدات والمستندات تُعرض كقوائم أفقية بفواصل خطية بسيطة (`divider` color)، مع أيقونة صغيرة يسار/يمين حسب الاتجاه (RTL/LTR)
- **عنصر بصري متكرر (Brand motif):** ركن ورقة مطوي بسيط (corner-fold shape) يظهر بشكل خفيف في: شاشة البداية (Splash)، أيقونة التطبيق، وكخلفية زخرفية خفيفة جداً (Opacity منخفضة) بشاشة "لا توجد مستندات بعد" (Empty state)
- **المسافات:** استخدم نظام مسافات ثابت (spacing scale: 4, 8, 16, 24, 32) بدل قيم عشوائية متفرقة بكل شاشة
- **زوايا العناصر (Border radius):** زاوية واحدة ثابتة صغيرة (8px) للحقول والأزرار فقط — بدون مزيج زوايا مختلفة بنفس الشاشة

## 4. دعم RTL/LTR

- كل الأيقونات الاتجاهية (سهم رجوع، ترتيب الصفحات بالسحب) يجب أن تنعكس تلقائياً بشكل صحيح بين العربي والإنجليزي عبر `Directionality` القياسية بفلاتر — تحقق يدوياً من كل شاشة بعد البناء بكلا الاتجاهين
- محاذاة النصوص: يمين لليمين بالعربي، يسار لليسار بالإنجليزي — بدون توسيط نصوص كحل وسط

## 5. حالات خاصة (Empty states, Errors, Loading)

- **شاشة فارغة (لا مستندات/لا مجلدات):** رسالة موجهة للفعل ("ابدأ بمسح أول مستند" بدل "لا يوجد شيء هنا")، مع أيقونة ركن الورقة المطوي بشفافية خفيفة، وزر واضح للإجراء المباشر
- **حالات الخطأ:** رسالة تشرح ماذا حصل وكيف يُصلح، بدون اعتذار أو غموض (مثال: "تعذر قراءة النص من هذه الصورة — جرب إضاءة أفضل" بدل "حدث خطأ ما")
- **التحميل:** مؤشر تحميل بسيط بلون `accentScan` (يربط بصرياً بخط المسح)، بدون أنيميشن زخرفي إضافي

## 6. ما يجب تجنبه صراحة

- الكروت المتطابقة بظلال رمادية موحدة كنمط أساسي للقوائم
- تدرجات لونية (Gradients) كخلفيات أو زخرفة
- تسميات بأحرف كبيرة بالكامل (ALL CAPS) للتصنيفات
- أيقونات "→" ملحقة بنهاية أزرار أو روابط
- أي عنصر تصميمي لا يخدم غرضاً وظيفياً واضحاً بالشاشة

## 7. تعليمات للتنفيذ

- طبّق نظام الألوان والخطوط عبر `ThemeData` مركزي واحد بملف `lib/theme/app_theme.dart`، ولا تكرر قيم الألوان يدوياً بملفات الشاشات
- بعد التطبيق، التقط لقطات شاشة (إن أمكن ضمن بيئة التطوير) لمراجعة الاتساق البصري عبر الشاشات الاثنتي عشرة قبل اعتبار المهمة منتهية
- إذا وُجد تعارض بين هذه المواصفات ومكتبة جاهزة (مثل شكل افتراضي من `signature` أو `cunning_document_scanner`) لا يمكن تخصيصه بسهولة، وثّق ذلك بدل تجاهله بصمت
