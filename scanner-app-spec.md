# مواصفات مشروع: تطبيق سكانر مستندات (Flutter) — نسخة كاملة قوية

> هذا المستند مخصص لتسليمه إلى Claude Code لبناء التطبيق كاملاً بدون تدخل إضافي من المطور. يحتوي كل القرارات النهائية، المزايا، البنية التقنية، وشاشات التطبيق.

---

## 1. نظرة عامة على المشروع

- **النوع:** تطبيق موبايل (Flutter) لمسح، تنظيم، وأرشفة المستندات — بديل بدفعة واحدة لتطبيقات مثل CamScanner / Adobe Scan.
- **المنصة:** Android أولاً (باستخدام Flutter بحيث iOS ممكن لاحقاً بدون إعادة كتابة).
- **النموذج التجاري:** تجربة مجانية كاملة لمدة 7 أيام (كل المزايا مفتوحة)، ثم قفل كامل للتطبيق حتى يدفع المستخدم **مبلغ صغير لمرة واحدة** عبر Google Play Billing (In-App Purchase من نوع **Non-consumable**) لفتح التطبيق مدى الحياة.
- **قيد صارم:** **بدون أي Backend أو سيرفر خارجي مهما كان السبب.** كل شي يعمل محلياً (on-device) بالكامل. لا حسابات، لا تسجيل دخول بإيميل، لا مزامنة سحابية إجبارية.
- **الخصوصية كنقطة بيع أساسية:** المستخدم يجب أن يفهم أن بياناته لا تغادر جهازه أبداً إلا إذا هو بنفسه صدّرها.

---

## 2. حزمة التقنيات (Tech Stack)

| الغرض | المكتبة/التقنية المقترحة |
|---|---|
| قاعدة بيانات محلية | `Hive` أو `Isar` (تخزين بيانات المستندات والمجلدات) |
| تخزين آمن للمفاتيح/كلمات السر | `flutter_secure_storage` |
| التشفير (للنسخ الاحتياطي والملفات الحساسة) | `encrypt` (AES-256) |
| كشف حواف المستند وتصحيح المنظور | `edge_detection` أو مكافئ (أو حل native عبر OpenCV إذا لزم) |
| تحسين الصورة (فلاتر، إزالة الظل) | معالجة صور محلية (`image` package أو معالجة native) |
| OCR (عربي + إنجليزي) | `google_mlkit_text_recognition` (on-device, مجاني) |
| إنشاء/تعديل PDF | `pdf` + `printing` (Dart packages) |
| تحويل PDF إلى Word | مكتبة تحويل محلية أو حل مدفوع بترخيص لمرة واحدة (بدون استدعاء API خارجي مدفوع لكل استخدام) |
| ضغط PDF | معالجة محلية لإعادة ضغط الصور داخل الملف |
| القفل المحلي (PIN/بصمة) | `local_auth` + شاشة PIN مخصصة |
| الإشعارات المحلية | `flutter_local_notifications` |
| التوقيع اليدوي | `signature` package |
| المشاركة | `share_plus` |
| الطباعة | `printing` package (يدعم طابعات AirPrint/WiFi) |
| اختصار الشاشة الرئيسية / Quick Tile | `quick_actions` (iOS/Android shortcuts) + Android Quick Settings Tile عبر كود native بسيط |
| الدفع داخل التطبيق | `in_app_purchase` (Google Play Billing) |
| اختيار صور من المعرض | `image_picker` |

**لا حاجة إطلاقاً لأي مكتبة اتصال بسيرفر خارجي (لا `http`, لا `dio` لأي API مدفوع، لا Firebase).**

---

## 3. بنية البيانات (Data Model) — محلي بالكامل

```
Folder
 ├─ id
 ├─ name (حر، يحدده المستخدم)
 ├─ colorTag (اختياري)
 ├─ createdAt

Document
 ├─ id
 ├─ folderId
 ├─ name (يُقترح تلقائياً من OCR، قابل للتعديل)
 ├─ pages: List<Page>
 ├─ extractedText (نتيجة OCR الكاملة، لأغراض البحث)
 ├─ createdAt
 ├─ colorTag (اختياري)
 ├─ manualValidUntilNote (نص حر اختياري، بدون تنبيهات تلقائية)
 ├─ locationNote (نص حر اختياري)

Page
 ├─ id
 ├─ documentId
 ├─ imagePathHighRes
 ├─ imagePathLowRes (نسخة مضغوطة للمشاركة السريعة)
 ├─ order (لإعادة الترتيب بالسحب)

AppSettings
 ├─ pinHash / biometricEnabled
 ├─ autoLockMinutes (افتراضي 15)
 ├─ lastBackupDate
 ├─ trialStartDate
 ├─ isPurchased (bool)
 ├─ viewMode (grid/list)
```

كل شي مخزّن محلياً عبر Hive/Isar. الصور تُخزَّن في مجلد التطبيق الخاص (App Sandbox)، ليس في المعرض العام، لضمان الخصوصية.

---

## 4. قائمة المزايا الكاملة (نسخة الإطلاق — لا تحديثات مؤجلة)

### أ. السكانر الذكي
1. كشف حواف تلقائي وتصحيح زاوية التصوير (Perspective correction)
2. فلاتر تحسين: أبيض وأسود، ألوان، تلقائي
3. تنظيف الخلفية وإزالة الظل قدر الإمكان
4. تصوير صفحات متعددة بجلسة واحدة (Multi-page capture)
5. مؤقت ذاتي 3 ثواني للتصوير (Timer) لتقليل الاهتزاز
6. تكبير/معاينة الصورة قبل الحفظ النهائي للتأكد من وضوح النص
7. استيراد صورة موجودة من المعرض (مو بس تصوير مباشر) وتطبيق نفس المعالجة عليها
8. اقتراح تلقائي "صور الوجه الثاني" عند اكتشاف تصوير بطاقة/هوية صغيرة

### ب. تنظيم المستندات
9. دمج عدة صور بملف PDF واحد
10. إعادة ترتيب الصفحات بالسحب والإفلات داخل نفس المستند
11. حذف/إضافة صفحة لمستند موجود
12. تقسيم PDF كبير لملفات أصغر
13. نسخ مستند لمجلد آخر بدون نقل (Copy مو Move)
14. دمج مجلدين مختلفين إلى مجلد واحد
15. اقتراح تجميع الصور الملتقطة بفترة زمنية قصيرة (جلسة واحدة) في نفس المستند/المجلد

### ج. الأرشفة الآمنة
16. مجلدات حرة بالكامل (المستخدم يسميها كيف يريد — بدون فئات جاهزة مسبقاً)
17. قفل محلي كامل للتطبيق (PIN أو بصمة/Face ID)
18. قفل تلقائي بعد 15 دقيقة خمول (قابل للتعديل بالإعدادات)
19. بحث سريع بالاسم وبمحتوى النص المستخرج (OCR) عبر كل المجلدات
20. عرض شبكي/قائمة قابل للتبديل (Grid/List toggle)
21. اختيار متعدد للمستندات لتنفيذ إجراء جماعي (حذف/نقل/تصدير دفعة واحدة)
22. سجل نشاط بسيط (Timeline) يعرض آخر المستندات التي تم التعامل معها
23. تلوين/وسم المجلدات والمستندات بلون مميز اختياري
24. اقتراح اسم تلقائي للمستند الجديد بالاعتماد على النص المستخرج من OCR
25. كشف مستندات مكررة **بالاعتماد على تطابق النص المستخرج (OCR) وليس شكل الصورة**، وضمن نفس المجلد فقط — تفادياً لتنبيهات خاطئة بين مستندات متشابهة الشكل لكن مختلفة بالمحتوى (أرقام/تواريخ مختلفة)

### د. النسخ الاحتياطي والاسترجاع (بدون سيرفر)
26. **بدون أي حساب أو إيميل أو تعريف بالـ IP.** الاسترجاع فقط عبر ملف نسخة احتياطية مشفر يصدّره المستخدم بنفسه.
27. زر "إنشاء نسخة احتياطية" يطلب من المستخدم تحديد كلمة سر خاصة بالنسخة، ثم يصدّر كل البيانات كملف واحد مشفر (AES) يحفظه المستخدم أينما شاء (Drive شخصي، إيميل، فلاش)
28. زر "استرجاع من نسخة احتياطية" يطلب رفع الملف وإدخال كلمة السر لفك التشفير محلياً
29. نسخ احتياطي محلي تلقائي دوري (يُحفظ بمجلد يختاره المستخدم على جهازه، بدون رفع لأي سيرفر) + تذكير دوري (كل شهرين تقريباً) عبر إشعار محلي "حدّث نسخة احتياطية"
30. تحذير واضح للمستخدم عند أول استخدام: فقدان الجهاز/القفل بدون نسخة احتياطية = فقدان دائم للبيانات (هذا مقصود لحماية الخصوصية الكاملة)

### هـ. استخراج النص (OCR)
31. استخراج نص من أي صورة مستند (عربي + إنجليزي) عبر Google ML Kit، محلياً بالكامل
32. نسخ النص المستخرج مباشرة (للصق بواتساب، ملاحظات، إلخ)
33. البحث داخل محتوى كل المستندات عبر النص المستخرج (مو بس اسم الملف)

### و. تحويل الصيغ
34. تحويل صورة/PDF إلى Word (قابل للتعديل)
35. ضغط حجم PDF الكبير (تصغير الحجم بأقل فقدان جودة ممكن)
36. تحويل PDF إلى صور منفصلة لكل صفحة
37. تصدير بمقاس ورق قياسي احترافي (A4) بدل أبعاد كاميرا عشوائية
38. تصدير بجودتين عند المشاركة: جودة عالية (أرشفة/طباعة) وجودة خفيفة (إرسال سريع بالواتساب)

### ز. التوقيع الإلكتروني
39. رسم توقيع بالإصبع (أو قلم/Stylus إن توفر) وحفظه كـ"توقيعي" لإعادة الاستخدام
40. وضع التوقيع على أي PDF بالسحب لتحديد الموقع بالصفحة
41. إضافة تاريخ تلقائي اختياري بجانب التوقيع
42. حماية أي PDF مُصدَّر بكلمة سر (PDF password protection)

### ح. المشاركة والطباعة
43. مشاركة مباشرة لأي تطبيق مثبت (واتساب، إيميل، تيليجرام...) عبر `share_plus`
44. طباعة مباشرة لطابعات WiFi/Bluetooth القريبة
45. وضع "مسح سريع وشارك" يتخطى خطوة الحفظ بالأرشيف للاستخدام السريع لمرة واحدة
46. تصدير دفعة (Batch export) لعدة مستندات مختلطة الصيغ دفعة واحدة
47. تصدير فهرس نصي بأسماء كل المستندات ضمن مجلد معين

### ط. سهولة الوصول والتفاعل
48. اختصار من الشاشة الرئيسية (Home Screen Shortcut) لفتح الكاميرا مباشرة
49. Quick Settings Tile (أندرويد) لفتح الكاميرا من أي شاشة بدون فتح التطبيق كاملاً
50. ملاحظة نصية حرة اختيارية لكل مستند (بدون تنبيهات تلقائية) لتاريخ "صالح لغاية" يحدده المستخدم بنفسه
51. حقل ملاحظة موقع/مصدر اختياري لكل مستند (نص حر)

---

## 5. مزايا مرفوضة صراحة — لا تُنفَّذ

- فئات/تصنيفات جاهزة مسبقاً للمجلدات (تم استبدالها بمجلدات حرة بالكامل)
- تنبيهات تلقائية لانتهاء صلاحية الوثائق (تم استبدالها بحقل ملاحظة يدوي فقط، بند 50)
- قفل مزدوج للمجلدات الحساسة
- وضع إخفاء سريع/Panic mode
- أي تعريف أو استرجاع حساب عبر عنوان IP
- تصنيف تلقائي بالذكاء الاصطناعي لنوع المستند (AI auto-categorization)
- قراءة الباركود/QR

---

## 6. نموذج التسعير والتفعيل

1. عند أول فتح للتطبيق، يُسجَّل `trialStartDate` محلياً.
2. طوال 7 أيام من هذا التاريخ: **كل المزايا بلا استثناء مفتوحة بالكامل** — لا قيود على أي ميزة.
3. بعد انتهاء 7 أيام وإذا `isPurchased == false`: تظهر شاشة قفل كاملة لا يمكن تجاوزها، تعرض السعر وزر شراء واحد واضح.
4. الشراء عبر `in_app_purchase` — منتج واحد فقط، نوع **Non-consumable**، سعر صغير يحدده أحمد لاحقاً بحساب Play Console.
5. بعد الشراء الناجح: `isPurchased = true` يُخزَّن محلياً، ويتحقق التطبيق من حالة الشراء عبر Google Play API المحلي (restore purchases) في حال حذف التطبيق وإعادة تثبيته بنفس حساب Google.
6. لا حاجة لأي سيرفر تحقق — Google Play Billing يتولى هذا بالكامل من طرفه.

---

## 7. شاشات التطبيق (Screens)

1. **Splash Screen** — يتحقق من حالة القفل (trial/purchased) وحالة PIN.
2. **شاشة إعداد القفل (أول استخدام فقط)** — تحديد PIN أو تفعيل البصمة.
3. **شاشة القفل (Lock Screen)** — تظهر عند فتح التطبيق أو بعد 15 دقيقة خمول.
4. **الشاشة الرئيسية** — قائمة المجلدات (Grid/List toggle)، زر بحث علوي، زر "+" لإنشاء مجلد أو مسح سريع.
5. **شاشة داخل المجلد** — قائمة المستندات، زر اختيار متعدد، زر إضافة مستند جديد.
6. **شاشة الكاميرا/المسح** — كشف حواف حي، تصوير متعدد، معاينة قبل الحفظ.
7. **شاشة تفاصيل المستند** — عرض الصفحات، إعادة ترتيب، أزرار (مشاركة، طباعة، توقيع، OCR، تحويل صيغة، حذف/نقل/نسخ).
8. **شاشة OCR/النص المستخرج** — عرض النص، زر نسخ.
9. **شاشة التوقيع** — رسم التوقيع، وضعه على الصفحة.
10. **شاشة الإعدادات** — تغيير القفل، مدة القفل التلقائي، إنشاء/استرجاع نسخة احتياطية، حالة الاشتراك/الشراء، وضع العرض الافتراضي.
11. **شاشة الشراء (Paywall)** — تظهر بعد انتهاء التجربة أو عند اختيار "الشراء الآن" من الإعدادات.
12. **شاشة سجل النشاط (Timeline)** — آخر المستندات المضافة/المعدَّلة.

---

## 8. متطلبات غير وظيفية (Non-functional)

- التطبيق يعمل بالكامل **Offline** — لا يحتاج إنترنت لأي ميزة أساسية (الإنترنت مطلوب فقط لعملية الشراء نفسها عبر Google Play).
- الأداء: معالجة الصور والـ OCR يجب أن تتم بدون تجميد الواجهة (استخدام `compute()` أو Isolates للعمليات الثقيلة).
- دعم اللغة العربية بالكامل بالواجهة (RTL) بالإضافة للإنجليزية.
- التعامل الآمن مع الأذونات: الكاميرا، التخزين، المعرض — طلبها فقط وقت الحاجة الفعلية مع شرح مبرر واضح للمستخدم.

---

## 9. تعليمات للتنفيذ (لـ Claude Code)

- ابدأ بإنشاء هيكل مشروع Flutter نظيف (`lib/models`, `lib/services`, `lib/screens`, `lib/widgets`).
- نفّذ طبقة البيانات (Hive models + services) أولاً، ثم الشاشات، ثم اربط منطق التجربة/الشراء أخيراً.
- كل ميزة بالقسم 4 يجب أن تُنفَّذ فعلياً وتُختبر يدوياً بمنطق بسيط (لا حاجة لكتابة اختبارات آلية شاملة إلا إذا طُلب لاحقاً).
- لا تضف أي استدعاء شبكة (`http`, Firebase, أو أي API خارجي) في أي جزء من الكود إطلاقاً باستثناء مكتبة `in_app_purchase` نفسها (وهي تتصل بمتجر Google مباشرة وليست سيرفراً خاصاً بالمطور).
- عند الشك بأي تفصيل غير مذكور هنا، اختر الحل الأبسط والأكثر اتساقاً مع مبدأ "بدون سيرفر، خصوصية كاملة، دفعة واحدة".
